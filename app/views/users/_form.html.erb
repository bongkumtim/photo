<%= form_for @user, html: { :'data-type' => 'json', multipart: true } do |f| %>
  <p>
    <label>My Avatar</label>
    <%= f.file_field :avatar %>
    <%= f.text_field :name %>
    <%= f.text_field :description %>
  </p>
  	<%= f.submit "Create" %>
<% end %>